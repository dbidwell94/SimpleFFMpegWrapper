namespace FFMpegWrapper.Options
{
    /// <summary>
    /// This enum maps to the -cpu-used parameter of FFMpeg
    /// </summary>
    public enum CpuUsed
    {
        One,
        Half,
        AllAvailable
    }
}