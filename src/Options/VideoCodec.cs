namespace FFMpegWrapper.Options
{
    /// <summary>
    /// This enum maps to the -c:v parameter of FFMpeg
    /// </summary>
    public enum VideoCodec
    {
        vp8,
        vp9,
        mov,
        avi,
        flv,
        copy
    }
}